name: Test User List
on: [workflow_dispatch]

jobs:
  list-users:
    runs-on: macos-latest
    steps:
      - name: Print real users only
        run: |
          dscl . list /Users | grep -v -E "^_|^daemon|^nobody|^root"
