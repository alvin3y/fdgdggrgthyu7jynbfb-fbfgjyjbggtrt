name: Native VNC Stream (noVNC)

on: 
  workflow_dispatch:

jobs:
  vnc-stream:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # 1. Install Dependencies
      - name: Install Tools
        run: |
          # Install Cloudflare Tunnel
          brew install cloudflared
          # Install websockify via Python
          pip3 install websockify

      # 2. Download noVNC Web Client
      # We need the HTML/JS files to actually render the screen in the browser
      - name: Setup noVNC
        run: |
          git clone https://github.com/novnc/noVNC.git
          cd noVNC
          git checkout v1.4.0
          # Link the VNC standard template to index.html so it loads by default
          ln -s vnc.html index.html
          cd ..

      # 3. Enable Native macOS Screen Sharing
      - name: Enable macOS VNC Server
        run: |
          echo "Enabling Screen Sharing..."
          # Set the password to 'password'
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart \
            -activate -configure -access -on \
            -clientopts -setvnclegacy -vnclegacy yes \
            -clientopts -setvncpw -vncpw password \
            -restart -agent -privs -all

      # 4. Start websockify
      # Bridges VNC (5900) to WebSocket (6080) and serves the noVNC folder as a website
      - name: Start Websockify
        run: |
          # --web points to the folder we just cloned
          websockify --web=./noVNC 6080 localhost:5900 &
          echo "Websockify running on port 6080"

      # 5. Expose to Internet via Cloudflare Tunnel
      - name: Start Tunnel & Stream
        run: |
          echo "Starting Cloudflare Tunnel..."
          cloudflared tunnel --url http://localhost:6080 > tunnel.log 2>&1 &
          
          # Wait for the URL to be generated
          sleep 10
          TUNNEL_URL=$(grep -o 'https://.*\.trycloudflare.com' tunnel.log | head -n 1)
          
          echo "--------------------------------------------------------"
          echo "🔥 DESKTOP LIVE AT: $TUNNEL_URL/index.html?password=password&autoconnect=true"
          echo "--------------------------------------------------------"
          
          # Keep the job alive so you can connect
          while true; do sleep 60; done
